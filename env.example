# ============================================
# Medusa Storefront Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# cp env.example .env.local

# ============================================
# REQUIRED - Medusa Backend Configuration
# ============================================

# Medusa Publishable API Key (REQUIRED)
# Get this from: Medusa Admin → Settings → Developer → Publishable API Keys
# Or create via API: POST /admin/api-keys with type: "publishable"
# This key is checked at startup and the app will fail without it
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_your_publishable_key_here

# Medusa Backend URL
# Default: http://localhost:9000 (for local development)
# For production: https://your-medusa-backend.com
# For Docker/K8s: http://medusa-backend:9000 (internal service name)
MEDUSA_BACKEND_URL=http://localhost:9000

# ============================================
# OPTIONAL - Storefront Configuration
# ============================================

# Base URL for the storefront
# Used for metadata, sitemap, and canonical URLs
# Default: https://localhost:8000
# For production: https://your-storefront-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:8000

# Default region/country code
# Used by middleware for region selection
# Default: "us"
# Examples: "us", "gb", "de", "fr"
NEXT_PUBLIC_DEFAULT_REGION=us

# ============================================
# OPTIONAL - Payment Integration (Stripe)
# ============================================

# Stripe Publishable Key
# Get this from: https://dashboard.stripe.com/apikeys
# Required if you want to use Stripe for payments
NEXT_PUBLIC_STRIPE_KEY=pk_test_your_stripe_publishable_key

# Alternative: Medusa Payments (if using Medusa's payment system)
# NEXT_PUBLIC_MEDUSA_PAYMENTS_PUBLISHABLE_KEY=pk_medusa_payments_key
# NEXT_PUBLIC_MEDUSA_PAYMENTS_ACCOUNT_ID=acct_medusa_account_id

# ============================================
# OPTIONAL - Image Hosting (S3/Cloud Storage)
# ============================================

# Medusa Cloud S3 Configuration
# Only needed if using Medusa Cloud for image hosting
# MEDUSA_CLOUD_S3_HOSTNAME=your-s3-bucket.s3.amazonaws.com
# MEDUSA_CLOUD_S3_PATHNAME=/your-path-prefix

# ============================================
# OPTIONAL - Deployment/SEO
# ============================================

# Vercel URL (for sitemap generation)
# Automatically set by Vercel, but you can override
# NEXT_PUBLIC_VERCEL_URL=https://your-app.vercel.app

# ============================================
# NOTES
# ============================================
# - All NEXT_PUBLIC_* variables are exposed to the browser (client-side)
# - Variables without NEXT_PUBLIC_ are server-side only
# - Never commit .env.local to git (it's in .gitignore)
# - For production, set these in your deployment platform (K8s ConfigMap/Secrets, Vercel, etc.)
